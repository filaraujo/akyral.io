<div drawer parallax>
    <h2>User Interface</h2>
    <ul>
        {{#each components}}
            {{#is component.disabled undefined}}
                <li>
                    <a href="{{component.name}}.html">
                        {{component.name}}
                        <span>akyral-{{component.name}}</span>
                    </a>
                </li>
            {{/is}}
        {{/each}}
        {{#each components}}
            {{#isnt component.disabled undefined}}
                <li>
                    <a href="#">
                        {{component.name}}
                        <span>in development</span>
                    </a>
                </li>
            {{/isnt}}
        {{/each}}
    </ul>

</div>
